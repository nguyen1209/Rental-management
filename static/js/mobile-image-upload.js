document.querySelectorAll('[data-image-uploader]').forEach(uploader=>{const inputs=[...uploader.querySelectorAll('[data-image-input]')],preview=uploader.querySelector('[data-preview]'),wrap=uploader.querySelector('[data-preview-wrap]'),placeholder=uploader.querySelector('[data-placeholder]'),clear=uploader.querySelector('[data-clear]'),meta=uploader.querySelector('[data-file-meta]'),url=uploader.querySelector('[data-url-input]'),remove=uploader.querySelector('[data-remove-image]');let objectUrl='';const show=file=>{if(!file)return;if(file.size>5*1024*1024){alert('Ảnh vượt quá 5 MB. Vui lòng chọn ảnh nhỏ hơn.');inputs.forEach(input=>input.value='');return}if(objectUrl)URL.revokeObjectURL(objectUrl);objectUrl=URL.createObjectURL(file);preview.src=objectUrl;preview.hidden=false;placeholder.hidden=true;clear.hidden=false;wrap.classList.add('has-image');meta.textContent=`Đã chọn: ${file.name} · ${(file.size/1024/1024).toFixed(1)} MB`;meta.classList.add('is-ready');remove.value='0';if(url)url.value='';inputs.forEach(input=>{if(input.files[0]!==file)input.value=''})};inputs.forEach(input=>input.addEventListener('change',()=>show(input.files[0])));clear.addEventListener('click',()=>{inputs.forEach(input=>input.value='');if(objectUrl)URL.revokeObjectURL(objectUrl);objectUrl='';preview.src='';preview.hidden=true;placeholder.hidden=false;clear.hidden=true;wrap.classList.remove('has-image');meta.textContent='PNG, JPG, GIF hoặc WEBP · tối đa 5 MB';meta.classList.remove('is-ready');remove.value='1';if(url)url.value=''})});
